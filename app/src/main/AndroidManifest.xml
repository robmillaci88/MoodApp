<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robmillaci.MoodTracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.robmillaci.MoodTracker.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.robmillaci.MoodTracker.com.example.robmillaci.MoodTracker.MainActivity" />
        </activity>

        <receiver
            android:name="com.example.robmillaci.MoodTracker.Schedule"
            android:enabled="true" />

        <activity android:name=".History" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".HistoryGraph"
            android:parentActivityName=".History">
        </activity>
    </application>

</manifest>